export const canvasSize = {
  width: 1080,
  height: 1350,
};

export const minZoom = 0.75;
export const maxZoom = 8;

// Debug tools: zoom, pan, fullscreen, and seed coordinate logging.
export const enablePreviewDebug = import.meta.env.DEV;
